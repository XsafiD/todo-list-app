# ── Database (MySQL via Docker: make mysql-up) ──
DB_HOST=localhost
DB_PORT=3306
DB_NAME=dashboardku
DB_USER=dashboardku
DB_PASS=secret

# ── Flask ──
SECRET_KEY=change-me-generate-with-openssl-rand-hex-32
DEBUG=true
FLASK_APP=app:create_app
FLASK_DEBUG=1
